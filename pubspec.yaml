name: form_with_flutterbloc
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^1.2.5
  flutter_bloc: ^6.1.1
  formz: ^0.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true